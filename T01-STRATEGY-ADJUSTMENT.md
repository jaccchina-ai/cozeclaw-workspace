# T01 策略调整说明

## 新卖出策略规则

根据用户指示，调整 T+1 竞价选股的卖出策略如下：

### T+2 日处理
- **如果涨停**: 卖出一半仓位，持有另一半
- **如果不涨停**: 收盘价全部卖出

### T+3 日处理（仅当 T+2 日涨停并持有一半仓位时）
- **如果涨停**: 继续持有剩余仓位
- **如果不涨停**: 收盘价卖出剩余一半

### T+4 日及以后处理
- 以此类推，每个交易日重复上述逻辑，直到全部卖出

## 对胜率和盈利统计的影响

### 胜率计算方式变更
- **原规则**: 只要 T+2 日收盘价 > T+1 日买入价即算盈利
- **新规则**: 需要跟踪每一笔卖出的盈利情况，综合计算整体胜率
- **新胜率定义**: 最终全部卖出后，总收益为正即算盈利

### 盈利统计变更
- **原规则**: 仅计算 T+2 日卖出的单次收益
- **新规则**: 需要计算多次卖出的平均收益
- **盈利计算方式**: (第一次卖出金额 + 第二次卖出金额 + ...) - 总买入金额

## 需要修改的核心模块

### 1. 数据库模型修改
```python
# 修改 tracked_results 表结构
ALTER TABLE tracked_results ADD COLUMN shares_held FLOAT DEFAULT 1.0;
ALTER TABLE tracked_results ADD COLUMN sell_history JSONB DEFAULT '[]';
ALTER TABLE tracked_results ADD COLUMN final_profit FLOAT;
```

### 2. 跟踪逻辑修改
```python
# 在 track.py 中更新卖出逻辑
# 增加每日检查涨停状态的逻辑
# 分批次记录卖出情况
```

### 3. 胜率和盈利统计修改
```python
# 在 stats.py 中更新胜率计算函数
def calculate_win_rate():
    # 根据最终盈利情况计算胜率
    pass

# 在 stats.py 中更新盈利统计函数
def calculate_average_profit():
    # 计算多次卖出的平均收益
    pass
```

### 4. 历史数据处理
- 选项 1: 仅对新数据应用新规则（推荐）
- 选项 2: 重新计算历史数据的胜率和盈利

## 实现步骤

1. **修改数据库结构**（预计 30 分钟）
2. **更新跟踪逻辑**（预计 2 小时）
3. **更新统计函数**（预计 1 小时）
4. **测试验证**（预计 1 小时）

## 潜在影响

- 现有任务 `track` 需要调整为每日运行，而不是仅在 T+2 日运行
- 需要新增定时任务来跟踪未全部卖出的股票
- 飞书通知内容需要调整，展示分批卖出的情况

是否需要我开始实施这些修改？